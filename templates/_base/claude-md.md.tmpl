# {{PROJECT_NAME}}

> {{PROJECT_DESCRIPTION}}

## 项目定位

本项目是一个 **{{PROJECT_TYPE}}** 项目，使用 **{{LANGUAGE}}** 开发。

## 路由规则

收到用户请求时，按以下意图路由：

| 用户意图 | 路由到 | 说明 |
|---|---|---|
{{ROUTING_TABLE}}

## 工作流

### 开发流程

```
需求理解 -> 任务分解 -> 编码实现 -> 测试验证 -> 代码审查 -> 提交
```

1. **需求理解** — 明确功能需求和验收标准
2. **任务分解** — 将需求拆分为可执行的小任务
3. **编码实现** — 按照编码规范实现功能
4. **测试验证** — 运行测试确保功能正确（覆盖率 >= 80%）
5. **代码审查** — 使用审查代理检查代码质量
6. **提交** — 遵循约定式提交格式

### 构建与测试

```bash
# 构建
{{BUILD_CMD}}

# 测试
{{TEST_CMD}}

# 代码检查
{{LINT_CMD}}

# 格式化
{{FORMAT_CMD}}
```

## 代理角色

| 代理 | 角色 | 何时使用 |
|---|---|---|
{{AGENT_TABLE}}

## 可用命令

| 命令 | 说明 |
|---|---|
{{COMMAND_TABLE}}

## 质量门禁

每次提交前必须通过：

| 检查项 | 方法 | 失败处理 |
|---|---|---|
| 构建通过 | `{{BUILD_CMD}}` | 修复编译错误 |
| 测试通过 | `{{TEST_CMD}}` | 修复失败的测试 |
| 代码检查 | `{{LINT_CMD}}` | 修复 lint 警告 |
| 覆盖率 >= 80% | 测试覆盖率报告 | 补充测试用例 |

## 错误处理

| 错误 | 处理方式 |
|---|---|
| 构建失败 | 检查错误日志，定位语法或依赖问题 |
| 测试失败 | 检查失败的测试用例，修复实现而非测试 |
| Lint 错误 | 运行 `{{FORMAT_CMD}}` 自动修复，手动处理剩余项 |

## 命名约定

- 文件名：`kebab-case`
- 分支名：`feat/xxx`、`fix/xxx`、`refactor/xxx`
- 提交格式：`<type>: <description>`
  - 类型：feat, fix, refactor, docs, test, chore, perf, ci

{{EXTRA_SECTIONS}}
