# Python 编码规范

> 适用于 {{PROJECT_NAME}} 项目的 Python 编码标准。

## 版本与风格

1. **Python 3.10+** — 使用最新稳定特性（match/case、类型联合 `X | Y`、参数规范）
2. **PEP 8 遵守** — 使用 `ruff` 或 `black` 自动格式化，行宽 88（black 默认）
3. **导入排序** — 使用 `isort` 或 `ruff` 的 isort 规则，标准库 > 第三方 > 本地

## 类型提示

4. **所有公开函数加类型注解** — 参数和返回值都标注类型
5. **使用 `mypy --strict`** — 启用严格类型检查
6. **优先使用内置泛型** — `list[str]` 而非 `List[str]`（Python 3.9+）
7. **使用 `TypeAlias`** — 复杂类型用类型别名提高可读性

## 变量与不可变性

8. **常量大写** — 模块级常量使用 `UPPER_SNAKE_CASE`
9. **优先使用不可变结构** — `tuple` 优于 `list`（不变时）、`frozenset` 优于 `set`
10. **dataclass 使用 `frozen=True`** — 数据类默认不可变

## 异步处理

11. **`async/await` 优先** — I/O 密集型操作使用 asyncio
12. **避免阻塞调用** — async 函数中不调用同步 I/O
13. **并发执行** — 独立任务使用 `asyncio.gather()` 或 `TaskGroup`

## 代码组织

14. **文件大小** — 单个文件不超过 400 行（最大 800 行）
15. **函数大小** — 单个函数不超过 50 行
16. **模块化** — 按功能域组织包，非按类型（`users/` 而非 `models/`）

## 命名约定

17. **snake_case** — 函数名、变量名、模块名
18. **PascalCase** — 类名、异常名
19. **UPPER_SNAKE_CASE** — 常量
20. **_前缀** — 私有属性和方法

## 错误处理

21. **具体异常** — 捕获具体异常类型，禁止裸 `except:`
22. **自定义异常** — 业务异常继承自定义基类，包含错误码
23. **上下文管理器** — 资源操作使用 `with` 语句
24. **永远不吞掉异常** — 至少记录日志

## 测试

25. **pytest** — 使用 pytest 作为测试框架
26. **测试文件命名** — `test_<module>.py`，放在 `tests/` 目录
27. **覆盖率要求** — 使用 `pytest-cov` 确保 80%+ 覆盖率
28. **fixtures 复用** — 使用 `conftest.py` 共享 fixture

## 安全

29. **输入验证** — 使用 Pydantic 进行 schema 验证
30. **参数化查询** — 禁止字符串拼接 SQL，使用 ORM 或参数绑定
31. **环境变量** — 密钥通过环境变量注入，禁止硬编码
32. **依赖审计** — 定期运行 `pip-audit` 或 `safety check`
