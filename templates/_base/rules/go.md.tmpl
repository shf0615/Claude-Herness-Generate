# Go 编码规范

> 适用于 {{PROJECT_NAME}} 项目的 Go 编码标准。

## 版本与工具

1. **Go 1.22+** — 使用最新稳定版本
2. **gofmt 强制** — 所有代码必须通过 `gofmt` 格式化（零容忍）
3. **golangci-lint** — 使用 `golangci-lint run` 进行综合静态分析

## 命名约定

4. **MixedCaps / mixedCaps** — 导出用 `PascalCase`，非导出用 `camelCase`
5. **短变量名** — 局部变量优先简短命名（`i`、`n`、`err`、`ctx`）
6. **接口命名** — 单方法接口用方法名 + `er` 后缀（`Reader`、`Writer`、`Closer`）
7. **包名小写** — 单词小写，不用下划线或 mixedCaps

## 错误处理

8. **显式检查错误** — 每个返回 error 的函数调用后立即检查 `if err != nil`
9. **错误包装** — 使用 `fmt.Errorf("context: %w", err)` 添加上下文
10. **自定义错误** — 使用 `errors.New` 或实现 `error` 接口的自定义类型
11. **永远不忽略错误** — 禁止 `_ = someFunc()`（除非有充分理由并注释）

## 并发

12. **goroutine 生命周期** — 启动的 goroutine 必须有明确退出路径
13. **context 传播** — 所有 I/O 操作接受 `context.Context` 作为第一个参数
14. **channel 优于 mutex** — 优先用 channel 通信，仅在必要时用 sync.Mutex
15. **sync.WaitGroup** — 等待多个 goroutine 完成

## 代码组织

16. **文件大小** — 单个文件不超过 800 行
17. **函数大小** — 单个函数不超过 50 行
18. **包设计** — 按功能域组织包，避免 `utils`、`common`、`helpers` 包名
19. **internal/** — 不对外暴露的包放在 `internal/` 目录

## 结构体与接口

20. **小接口** — 接口定义 1-3 个方法，消费者侧定义
21. **接受接口，返回结构体** — 函数参数用接口，返回值用具体类型
22. **指针接收器** — 修改状态的方法用指针接收器，只读方法用值接收器
23. **组合优于继承** — 使用嵌入（embedding）实现复用

## 测试

24. **表驱动测试** — 使用 `[]struct{ name string; ... }` 风格的测试用例
25. **子测试** — 使用 `t.Run(name, func(t *testing.T) { ... })`
26. **测试文件** — `*_test.go`，与源文件同包
27. **覆盖率要求** — 使用 `go test -cover` 确保 80%+ 覆盖率
28. **testify 可选** — 复杂断言可用 `testify/assert`，简单场景用标准库

## 安全

29. **输入验证** — 使用 `validator` 包或手动验证所有外部输入
30. **参数化查询** — 禁止字符串拼接 SQL，使用 `?` 占位符
31. **环境变量** — 密钥通过环境变量注入，禁止硬编码
32. **依赖审计** — 定期运行 `govulncheck`
