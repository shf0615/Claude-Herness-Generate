# C 编码规范

> 适用于 {{PROJECT_NAME}} 项目的 C 语言编码标准。

## 编译标准

1. **C11 标准** — 使用 `-std=c11` 编译，利用 C11 特性（`_Static_assert`、`_Atomic` 等）
2. **严格警告** — 编译时启用 `-Wall -Wextra -Werror -Wpedantic`
3. **静态分析** — 使用 `cppcheck --enable=all` 检查潜在问题

## 内存管理

4. **malloc/free 配对** — 每个 `malloc` 必须有对应的 `free`，在注释中标注所有权
5. **分配后检查** — `malloc` 返回值必须检查是否为 `NULL`
6. **优先栈分配** — 小型固定大小数据使用栈，仅在必要时堆分配
7. **初始化变量** — 声明时立即初始化，避免使用未初始化的变量
8. **Valgrind 零泄漏** — 所有测试必须通过 `valgrind --leak-check=full --error-exitcode=1`

## 命名约定

9. **函数名** — `snake_case`（如 `parse_config`、`send_response`）
10. **类型名** — `PascalCase` + `_t` 后缀（如 `ConfigParser_t`、`HttpResponse_t`）
11. **宏和常量** — `UPPER_SNAKE_CASE`（如 `MAX_BUFFER_SIZE`、`API_VERSION`）
12. **文件名** — `snake_case.c` / `snake_case.h`

## 头文件

13. **头文件守卫** — 使用 `#ifndef` / `#define` / `#endif` 防止重复包含
14. **最小化包含** — 头文件中只包含必需的其他头文件，前向声明优先
15. **公开 API** — 只在头文件中声明公开函数，内部函数使用 `static`

## 函数设计

16. **函数大小** — 单个函数不超过 40 行，超出则拆分为子函数
17. **参数 `const`** — 不修改的指针参数标记 `const`（如 `const char *str`）
18. **返回值检查** — 调用方必须检查所有可能失败的函数返回值
19. **错误码约定** — 成功返回 `0`，失败返回负数错误码，定义 `enum` 错误类型

## 代码组织

20. **文件大小** — 单个 `.c` 文件不超过 800 行
21. **`static` 限定** — 文件内部函数和全局变量使用 `static` 限制作用域
22. **禁止全局可变状态** — 避免非 `const` 全局变量，使用参数传递状态

## 安全

23. **缓冲区安全** — 使用 `snprintf` 替代 `sprintf`，使用 `strncpy` 替代 `strcpy`
24. **整数溢出** — 关键计算检查溢出，使用 `<stdint.h>` 固定宽度类型
25. **输入验证** — 所有外部输入（文件、网络、命令行参数）必须验证边界
26. **`assert` 不变量** — 使用 `assert()` 检查开发期的内部不变量

## 测试

27. **测试框架** — 使用 Unity 或 CUnit 进行单元测试
28. **测试文件命名** — `test_<module>.c`
29. **覆盖率要求** — 使用 `gcov`/`lcov` 确保 80%+ 覆盖率
30. **内存检查** — 每个测试用例通过 Valgrind 无泄漏验证
