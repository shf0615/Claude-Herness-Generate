# TypeScript 编码规范

> 适用于 {{PROJECT_NAME}} 项目的 TypeScript 编码标准。

## 类型系统

1. **启用严格模式** — `tsconfig.json` 中设置 `"strict": true`
2. **禁止 `any`** — 使用 `unknown` 替代，必要时用类型守卫缩窄
3. **显式返回类型** — 公开函数必须声明返回类型
4. **优先使用 `interface`** — 描述对象形状时用 `interface`，联合/交叉类型用 `type`
5. **使用可辨识联合** — 替代 `string` 枚举或魔术字符串

## 变量与常量

6. **`const` 优先** — 默认使用 `const`，仅在需要重新赋值时用 `let`，禁止 `var`
7. **不可变性** — 使用 `readonly` 标记不应修改的属性，优先使用 `ReadonlyArray<T>`

## 异步处理

8. **`async/await` 优先** — 避免原始 Promise 链和回调嵌套
9. **错误处理** — 每个 `await` 都应在 `try/catch` 中或有 `.catch()` 处理
10. **并行执行** — 独立的异步操作使用 `Promise.all()` 并行

## 代码组织

11. **导入顺序** — 外部依赖 > 内部模块 > 类型导入，各组之间空行分隔
12. **文件大小** — 单个文件不超过 400 行（最大 800 行），超出则提取模块
13. **函数大小** — 单个函数不超过 50 行，超出则拆分

## 命名约定

14. **PascalCase** — 类名、接口名、类型别名、枚举名
15. **camelCase** — 变量名、函数名、方法名、参数名
16. **UPPER_SNAKE_CASE** — 常量
17. **kebab-case** — 文件名和目录名

## 错误处理

18. **自定义错误类** — 继承 `Error` 并添加语义化属性（`code`、`statusCode`）
19. **永远不要吞掉错误** — 至少记录日志或重新抛出
20. **用户友好消息** — UI 层显示可读消息，服务端记录详细上下文

## 测试

21. **测试文件命名** — `*.test.ts` 或 `*.spec.ts`，与源文件同目录或 `__tests__/`
22. **覆盖率要求** — 最低 80%，关键路径 90%+
23. **模拟外部依赖** — 数据库、API、文件系统等使用 mock

## 安全

24. **输入验证** — 使用 Zod 或 Joi 进行 schema 验证
25. **参数化查询** — 禁止字符串拼接 SQL
26. **环境变量** — 密钥通过环境变量注入，禁止硬编码
